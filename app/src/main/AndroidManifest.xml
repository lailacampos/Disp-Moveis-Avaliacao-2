<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.laila.unifacs.avaliacao1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Avaliacao1">
        <activity
            android:name=".activity.CreditScreen"
            android:exported="true"
            android:label="@string/tela_creditos"
            android:theme="@style/myTheme"/>
        <activity
            android:name=".activity.ConfigScreen"
            android:exported="true"
            android:label="@string/tela_configuracao"
            android:theme="@style/myTheme"/>
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:label="@string/tela_inicial"
            android:theme="@style/myTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>